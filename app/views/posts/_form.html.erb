<%= simple_form_for @post, html: { multipart: true } do |f| %>
  <%= f.input :title %>
  <%= f.input :description, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} } %>

  <%= f.label :category %>
  <%= f.select :category, [ "Zdrowie", "Motywacja", "Dieta", "Rozwój" ], { :prompt => 'Select One' }, { class: "form-control", :id => "select" } %>
  <%= f.input :avatar, as: :file %>
  <%= f.button :submit %>
<% end %>