<%= simple_form_for @post, html: { multipart: true } do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :category %>
  <%= f.input :avatar, as: :file %>
  <%= f.input :tag_list%>
  <%= f.button :submit %>
<% end %>